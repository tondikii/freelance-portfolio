# TASKS.md — Daftar Task per Fase (Portfolio Tondiki)

> Cara kerja: kerjakan task BERURUTAN, satu per satu. Setiap task selesai → verifikasi (lint+build bila mengubah kode) → centang checkbox + tulis catatan singkat. BERHENTI dan minta konfirmasi user saat: (a) fase selesai, (b) keputusan besar di luar PRD, (c) butuh data yang hanya user tahu.

## FASE 1 — FONDASI: Design System, Konten, Kerangka

- [x] **T1. Generate & persist design system** — jalankan ui-ux-pro-max `--design-system --persist -p "Portfolio"` (query + dials di AGENTS.md §2). Review hasil: pastikan cocok untuk "professional trustworthy freelance portfolio, audiens UMKM Indonesia". Simpan sebagai `design-system/portfolio/MASTER.md`. Jika hasil kurang pas → retry sekali query lebih sempit → kalau tetap tidak, konfirmasi arah visual ke user SEBELUM lanjut.
  - DoD: MASTER.md ada & berisi pattern, palet, typography, spacing, motion, anti-pattern.

- [x] **T2. Terapkan token design system ke globals.css + fonts** — konversi output MASTER.md ke CSS variables Tailwind v4 di `src/app/globals.css`; pasang font via `next/font` (baca docs Next di `node_modules/next/dist/docs/` untuk API font versi ini); set metadata dasar + bahasa `id`. Baca `src/app/layout.tsx` & `src/app/page.tsx` yang ada sebelum mengubah.
  - DoD: build sukses, token terpakai (bukan hex lepas) di komponen dasar.

- [x] **T3. Struktur konten terpusat (`src/content/`)** — buat `site.ts` (nav, CTA, social, WhatsApp config), `profile.ts` (data PRD §5 + `PHOTO_SRC`), `demos.ts` (3 demo + URL + copy per-niche), `services.ts` (paket starter `[HARGA_STARTER]` + roadmap items), `experience.ts` (kurasi CV). Semua copy bahasa Indonesia manfaat-oriented. Dilarang mengarang fakta di luar PRD §5.
  - DoD: file ada, TypeScript valid, tidak ada angka/fakta fiktif.

- [x] **T4. Utilitas: WhatsApp link (`src/lib/wa.ts`)** — fungsi baku bikin `wa.me` URL dari konfigurasi di `site.ts` (nomor + template pesan per konteks CTA: umum, per-demo, konsultasi). Ekspor helper yang dipakai semua section.
  - DoD: unit logika benar (format nomor, encoding pesan), build lolos.

- [x] **T5. Placeholder foto & avatar** — generate `public/images/profile-placeholder.svg` (avatar inisial "TA" elegan sesuai palet MASTER); pastikan `PHOTO_SRC` di `profile.ts` dipakai satu-satunya sumber. Tambah TODO singkat (satu baris) di profile.ts cara mengganti foto + catat pengingat di Fase 2 T19.
  - DoD: placeholder tampil rapi di rasio 1:1, mudah diganti via satu baris.

- [x] **T6. Komponen motion reusable (`src/components/motion/`)** — `FadeIn`, `StaggerList`/`StaggerItem`, `transitions.ts` (durasi/easing terpusat, exit < enter). Semua hormat `useReducedMotion()`. Riset dulu: `"scroll reveal stagger" --domain gsap` + `"animation accessibility" --domain ux` lalu adaptasi ke `motion`.
  - DoD: demo internal sederhana (bisa dihapus), build lolos, reduced-motion bekerja.

- [x] **T7. Navbar + mobile menu** — sticky navbar dari `site.ts` (server component + sub-komponen client untuk menu & scroll state), CTA WhatsApp. Riset `"navigation landing sticky" --domain landing`. Smooth-scroll anchor + state aktif; mobile menu (sheet/accordion shadcn) dengan touch target ≥44px.
  - DoD: 375px OK, focus state terlihat, build lolos.

- [x] **T8. Footer** — minimal: copyright, link kontak/email, link portfolio lama, "dibangun dengan Next.js". Data dari `site.ts`.
  - DoD: konsisten token, responsive.

- [x] **T9. Verifikasi akhir Fase 1** — `npm run lint` + `npm run build` bersih; review visual mental 375/768/1024; update dokumen bila ada keputusan lintas-section baru (catat ke MASTER.md). **→ KONFIRMASI USER sebelum masuk Fase 2** (tampilkan ringkasan arah visual).

## FASE 2 — BUILD SECTIONS, POLISH & LAUNCH

- [x] **T10. Riset & ambil referensi Hero (pakai kuota 21st.dev)** — `get_usage` dulu, lalu `get_component` untuk kandidat terbaik dari PRD §4.2 (#10628 / #9037 / #5590 / #5127 — pilih 1–2 yang paling sesuai MASTER.md). Bandingkan, putuskan direction final (konfirmasi user jika dua kandidat sama kuat).
  - DoD: keputusan direction hero terdokumentasi singkat di sini.
  - Keputusan: kuota 2/2 dipakai untuk #5127 (grid interaktif + glow + trust stats) & #10628 (stagger entrance + avatar + scroll indicator). Direction final = hibrida: struktur latar interaktif dari #5127, koreografi entrance & avatar dari #10628. Adaptasi: grid via CSS gradient (bukan SVG rect) demi performa; glow via useMotionValue+useSpring (bukan setState); konten dari src/content; token MASTER.

- [x] **T11. Hero Section** — adaptasi referensi ke design system kita: headline/sub dari konten, CTA ganda (demo + WhatsApp), elemen interaktif (mouse-follow glow/dot grid — nonaktif di touch & reduced-motion), trust indicators (4+ th, 5k+ pengguna, 3 demo), foto placeholder via `PHOTO_SRC`. Semua stagger pakai wrapper T6.
  - DoD: tidak ada layout shift, animasi GPU-friendly, 375px OK, kontras ≥4.5:1, build lolos.

- [x] **T12. Demo Showcase (section konversi utama)** — 3 kartu dari `demos.ts`: thumbnail preview (opsi: screenshot demo via script Playwright — buat sendiri, lihat SKILLS.md §6.2 — atau styled mockup; JANGAN iframe live berat tanpa uji performa), tombol "Lihat Live Demo" tab baru (`noopener`), copy per-niche, CTA band penutup → WhatsApp per-demo (pakai wa.ts). Riset `"showcase grid hover" --domain landing`.
  - DoD: mobile stacking rapi, semua link berfungsi, build lolos.

- [x] **T13. Layanan & Paket Starter** — kartu paket utama (harga `[HARGA_STARTER]`), list deliverables, layanan lanjutan (booking/katalog/POS — "segera"). Riset `"pricing card trust" --domain landing`.
  - DoD: placeholder harga terlihat jelas sebagai placeholder, CTA WhatsApp.
  - Catatan: tanyakan harga asli ke user saat fase hampir selesai (lihat T19).

- [x] **T14. Roadmap Pertumbuhan** — visual timeline 3 tahap (Landing Page → Website+Fitur → Sistem Bisnis) — narasi "tumbuh bersama" (PRD §4.5). Riset `"timeline roadmap horizontal" --domain landing`.
  - DoD: mobile tidak menabrak (vertikal di <768px), animasi whileInView once.

- [x] **T15. Capabilities / Tech Stack** — dua grup: "untuk bisnismu" (awam) + chip teknis dari `experience.ts`/`profile.ts`. Ikon lucide konsisten; `aria-hidden` untuk dekoratif.
  - DoD: chip wrap rapi semua breakpoint, tidak ada jebakan kontras.

- [x] **T16. Tentang Saya + timeline pengalaman** — foto placeholder, narasi dari CV (PRD §5), timeline kurasi 4 posisi + pendidikan singkat. Riset `"about profile timeline" --domain landing`.
  - DoD: hanya fakta PRD §5, hierarki heading benar (h2/h3).

- [x] **T17. Proses Kerja** — 4 langkah (Konsultasi → Kustomisasi → Build & Revisi → Launch), icon + kartu. Copy menenangkan untuk UMKM awam.
  - DoD: build lolos, konsisten spacing rhythm MASTER.

- [x] **T18. Kontak/CTA Final + SEO & polish akhir** — section kontak (WA primer, email, area layanan), SEO metadata lengkap + Open Graph (og-image sederhana bisa dari placeholder/screenshot), cek hierarki heading tunggal h1, sitemap/robots jika docs Next menyarankan untuk versi ini, Lighthouse mental-check (font, ukuran gambar, tanpa CLS). Opsional (konfirmasi user): Vercel Analytics + dark mode.
  - DoD: `npm run lint` + `npm run build` bersih; semua section terhubung anchor dari navbar.

- [x] **T19. Serah terima ke user (data yang hanya user tahu)** — minta/ingatkan: (1) harga starter final → ganti `[HARGA_STARTER]`; (2) foto asli → ganti `PHOTO_SRC` + file di `public/images/`; (3) konfirmasi copy; (4) keputusan deploy (domain custom?). Jalankan build final setelah input masuk.
  - DoD: tidak ada placeholder tersisa selain yang sengaja ditunda, build final bersih.

- [x] **T20. Redesign visual (feedback user: terlalu basic & text-heavy)** — Hero dirombak jadi navy premium interaktif: mouse-spotlight + aurora gradients, word-reveal headline, kartu demo melayang parallax mouse, marquee tech strip; Demo Showcase: browser mockup berlapis + TiltCard 3D hover + feature chips (teks dipangkas); Services: benefit cards → icon rows ringkas; semua copy di src/content dipadatkan. Referensi kualitas: 21st.dev splite (spotlight) & hero-section-4 (minimal text). Kuota get_component habis (0/2) — dibangun sendiri dengan motion, tanpa dependency baru.

- [x] **T21. Redesign #2 (feedback user: tetap terasa SaaS, kurang foto, minta gaya portfolio personal)** — Hero split-screen TERANG ala 21st.dev #5260 (ravikatiyar) + #19074 (editorial collage): kiri "Halo, saya Tondiki" (personal) + 1 kalimat + CTA + trust chips; kanan FOTO BESAR (ilustrasi placeholder flat: diri + laptop) + 2 badge float (4+ th, 5.000+) + 2 mini browser-mockup overlap (collage). Placeholder foto diganti ilustrasi flat "foto menyusul". Demo Showcase: mockup browser diperbesar aspect 4/5, isi mockup diperkaya (hero bar, 3 kartu layanan, footer card) supaya photo-forward. Marquee dipindah ke strip bawah hero versi terang. TiltCard tidak dipakai lagi di showcase (ganti hover lift sederhana). Kuota 21st.dev 2/2 dipakai utk #5260 & #19074.

- [x] **T22. Redesign #3 (feedback: foto real, simple minimalist clean elegan, kurangi UI penuh)** — Hero ala 21st.dev hero-section-4: konten teks simpel di atas (avatar + headline + 1 kalimat + 2 CTA) lalu FOTO BESAR full-width (workspace real 21:9) + trust row polos. Marquee, floating badges, mini-mockup collage DIHAPUS. Showcase: kartu foto SCREENSHOT REAL 3 demo (Playwright capture dari URL live, di public/images/demos/) — UI kartu minimal (image + niche + judul + 1 baris + link). Mockup abstrak & tilt dihapus. Hero workspace = foto stok real (placeholder s/d foto user datang; ganti public/images/hero-workspace.jpg).

---

## Log Catatan

- (16/08/2026) T1: hasil generasi pertama (gaming style) ditolak; retry menghasilkan pattern Trust & Authority + palet navy/blue (dipakai); font EB Garamond diganti Plus Jakarta Sans via domain search. MASTER.md dikurasi manual dengan dokumentasi keputusan.
- (16/08/2026) T2: token diterapkan. Catatan keputusan: shadcn `accent` (hover bg halus) dipisah dari `cta` (biru #0369A1 untuk CTA/link) via token baru `--cta` agar semantik shadcn tidak rusak. Dark palette navy disiapkan (dark mode tetap nice-to-have). eslint config kini meng-ignore `.opencode/` & `design-system/` (error lint berasal dari script skill, bukan kode proyek).
- (16/08/2026) T4: wa.ts — 3 varian link (general/consultation/starter) + waDemoLink per demo; nomor format internasional tanpa "+" utk wa.me.
- (16/08/2026) T5: placeholder SVG avatar "TA" (navy + dots + inisial gradient), 512x512. TODO ganti foto ada di profile.ts; pengingat di T19.
- (16/08/2026) T6: transitions.ts (EASE_OUT/EASE_EXIT, durations 0.2/0.35/0.5, viewportOnce -80px, stagger 60ms) + FadeIn + StaggerGroup/Item; semua hormat useReducedMotion. Pedoman dari riset gsap/ux: y 16px, exit < enter, maks 1-2 elemen animasi per view.
- (16/08/2026) T7: navbar sticky (backdrop blur), active-link via IntersectionObserver (rootMargin -40%/-55%), mobile menu (AnimatePresence, Escape close, 44px target), CTA konsultasi via wa.ts; scroll-margin-top 4.5rem utk anchor di globals.css. Riset "navigation sticky landing" landing-domain: no match (dinyatakan), fallback: ux-domain "sticky header scroll" (3 hasil, dipakai).
- (16/08/2026) T8: footer 3 link kontak + copyright dinamis.
- (16/08/2026) T9: lint 0 error, build sukses, 4 halaman statis. FASE 1 SELESAI — menunggu konfirmasi user arah visual sebelum Fase 2.
- (16/08/2026) T10: kuota 21st.dev 2/2 dipakai utk #5127 & #10628. Direction hero = hibrida (latar interaktif dari #5127, entrance/avatar dari #10628), diadaptasi: grid CSS gradient, glow motion-value+spring, konten dari src/content.
- (16/08/2026) T11: hero.tsx — grid interaktif + glow mouse-follow (nonaktif utk touch & reduced-motion), avatar PHOTO_SRC spring, status pill, CTA ganda, trust stats, dl/dt semantik. Exit typo-transition diperbaiki.
- (16/08/2026) T12: demo-showcase — riset landing "showcase card grid hover" (3 hasil; pola portfolio-grid dipakai: visuals-first, usable-without-hover). Thumbnail = mockup browser-frame CSS (keputusan: tanpa iframe/screenshot demi performa; previewGradient di demos.ts; bisa diganti screenshot asli nanti). 2 CTA per kartu (live demo + WA per-demo) + CTA band navy.
- (16/08/2026) T13: services — kartu starter di-highlight border cta, harga tampil sbg [HARGA_STARTER] + note; 4 kartu manfaat utk awam; 3 layanan lanjutan badge "Segera" (border dashed).
- (16/08/2026) T14: riset "timeline roadmap steps" landing = no match (dinyatakan); fallback pola umum berlabel: 3 tahap, connector horizontal desktop/vertikal mobile, section navy, tahap 3 "(dalam persiapan)". Icon Growth tidak ada di lucide → Sprout.
- (16/08/2026) T15: capabilities — 4 kartu value awam + chip teknis per grup dari profile.ts.
- (16/08/2026) T16: about — foto placeholder besar, quote filosofi, 4 highlight qualitative social proof (testimoni fiktif tetap dilarang), timeline karier vertikal + 2 kartu pendidikan. Riset "about profile social proof": pedoman qualitative proof diterapkan.
- (16/08/2026) T17: process — 4 langkah bernomor besar, ikon lucide konsisten.
- (16/08/2026) T18: contact section navy (WA primer + email) + info card (nomor, email, lokasi, area layanan, jam respon). SEO: metadata lengkap (metadataBase, OG id_ID, keywords, robots), robots.ts + sitemap.ts tergenerate. siteConfig.siteUrl = TODO domain final (di site.ts). Og-image custom ditunda ke T19 (butuh domain final). Heading check: 1 h1 (hero) saja, section h2.
- (16/08/2026) T19: FASE 2 SELESAI — lint 0 error, build sukses (4 route statis). Menunggu input user: harga starter, foto asli, konfirmasi copy, keputusan domain/deploy, og-image.
- (16/08/2026) T20: feedback user "terlalu basic, tulisan mulu". Redesign: hero navy premium (spotlight mouse-follow ala 21st splite, aurora, word-stagger, floating demo cards parallax, marquee skills), demo cards browser-mockup + tilt 3D (useSyncExternalStore utk deteksi hover-device, hormat reduced-motion), copy dipangkas ~50% di semua content file. clientBenefits diganti serviceHighlights (icon rows). profile.headline → headlinePrimary+headlineAccent. Build & lint bersih.
- (16/08/2026) T21: feedback user ke-2 "masih SaaS, kurang foto, harusnya portfolio personal". Arah final: photo-forward & personal. Hero terang split-screen (ref #5260+#19074), headline "Halo, saya Tondiki / saya buatkan website bisnismu", foto besar (ilustrasi flat placeholder "foto menyusul"), badge float, mini-mockup collage. Showcase mockup 4:5 kaya visual. Section eyebrow "Showcase"→"Portfolio", judul→"Hasil Kerja Saya", CTA→"Kunjungi Demo", hero CTA→"Lihat Hasil Kerja Saya".
- (16/08/2026) T22: feedback user ke-3 "pakai foto real, perbanyak foto, simple minimal clean elegan, kurangi UI penuh". Hero final ala hero-section-4: teks di atas + foto workspace besar di bawah + trust row; semua ornamen dihapus. Screenshot real 3 demo via Playwright (npx playwright screenshot, chromium diinstall) → public/images/demos/{clinic,spa,wedding}.jpg. marqueeSkills & demo-icons dihapus. Catatan: model tidak bisa inspeksi visual gambar — user perlu cek screenshot.
- (16/08/2026) T23: foto profil ilustrasi diganti foto manusia asli lokal `public/images/profile-placeholder.jpg` sebagai placeholder; foto user asli tetap tinggal mengganti `PHOTO_SRC`. Playwright CLI dipakai untuk capture section relevan: 4 slide per demo (hero + treatment/package/gallery/location/contact/dokter), lalu `DemoGallery` memainkan slide otomatis tiap 4,5 detik dan berhenti otomatis saat reduced-motion. Layanan dipindah sebelum portfolio: Landing Page, Company Profile, Booking, Katalog, Mobile MVP, Custom Website; semua punya placeholder harga normal/diskon dan CTA pesan. Roadmap dihapus dari page dan file section. Copy capabilities dibuat lebih manusiawi dan ringkas.
+- (16/08/2026) T24: section "Cara saya bekerja" (Capabilities) dihapus dari halaman dan file. Tech stack (`skillGroups`) dipertahankan dan dipindah ke kolom kanan section Tentang (isi space kosong di bawah experience highlights) dengan heading "Teknologi yang saya gunakan".
- (16/08/2026) T25: layout bagian bawah section Tentang dirombak jadi grid 2 kolom — kiri "Perjalanan Karier" (timeline + pendidikan), kanan "Teknologi yang saya gunakan" (tech stack).
- (16/08/2026) T25b: blok "Pendidikan" dikeluarkan dari grid perjalanan karier menjadi section penuh sendiri di bawahnya.
- (16/08/2026) T26: toggle bahasa ID/EN (state-based, tanpa dependency baru). `LanguageProvider` (useSyncExternalStore + localStorage `lang`) di `src/lib/i18n.tsx`; seluruh copy dipindah ke `src/content/dictionary.ts` (objek `id` & `en`, tipe lebar via `WidenLiteral`); `wa.ts` hanya `waLink(message)` + `waDisplayNumber`; toggle ada di navbar. Semua section jadi client + `useI18n()`. Metadata SEO tetap ID (default).
- (16/08/2026) T26b: bug toggle bahasa — item di `StaggerGroup` yang `key`-nya berubah saat bahasa berganti (mis. `service.title`, `highlight.title`, `group.title`) di-remount React dan terjebak hidden (parent `whileInView` `once:true` sudah ter-trigger). Fix: tambah `id` stabil per item di `dictionary.ts` (services pakai `icon`, highlights/timeline/education/skillGroups/process pakai `id`) dan pakai sebagai `key`. Terverifikasi multi-toggle ID↔EN: kartu layanan 6/6, demo 3/3, tentang 55/55, proses 4/4, semua visible setelah toggle.
- (16/08/2026) T27: polish UI berdasarkan riset ui-ux-pro-max (`portfolio-grid`, `hero-centric`, `motion-driven`, reduced-motion, hover/card guidance) dan referensi metadata 21st.dev (#9037 Portfolio Hero, #10628 Hero Block, #5590 Animated Hero, #7517 Portfolio Gallery, #9607 Project Showcase, #6261 pricing). Hero dirombak menjadi editorial motion hero: grid halus, pointer spotlight spring, parallax workspace, magnetic CTA, avatar card, dan trust strip. Portfolio menjadi featured visual + supporting cards; gallery pause saat hover/focus dan reduced-motion. Layanan dipadatkan menjadi featured pricing card + kartu kebutuhan ringkas dengan harga provisional sedikit di bawah pasar: Landing 1,75jt; Company Profile 2,75jt; Booking 5,5jt; Catalog 4,25jt; Mobile MVP 9,5jt; Custom mulai 6,5jt. Scroll reveal section dibuat progressive enhancement (konten tidak lagi invisible sebelum viewport) untuk menghindari blank capture/bug toggle. Copy utama dipangkas dan di-humanize.
- (16/08/2026) T28: feedback lanjutan user. Referensi minimalist hero #4582 dipakai sebagai prinsip, bukan copy-paste: portrait jadi focal point, workspace jadi inset. Portfolio diubah dari featured-wide menjadi 3 kartu equal dengan rasio 16:10 yang terverifikasi (1.6). DemoGallery mendapat prev/next/play-pause, auto-rotate hanya saat visible, berhenti pada hover/focus/offscreen/tab-hidden/reduced-motion. ServiceCard mendapat spring tilt, hover/tap feedback, icon spring, dan shimmer periodik hanya pada featured card. About photo/cards, Process cards/icons, Contact CTA mendapat micro-interaction. Hero portrait ambient loop tetap nonaktif pada reduced-motion. Next 16 Image `priority` diganti `preload` untuk LCP portrait.
- (16/08/2026) T29: user meminta capture reference vs lokal. Playwright capture reference iframe 21st.dev menunjukkan komposisi portrait besar + aksen lingkaran + copy pendek; hero lokal diukur 837px pada viewport 1440x900 (tepat `100svh - navbar`) setelah headline dipangkas menjadi "Halo, saya Tondiki / website siap dipercaya.". Mobile 375px tidak overflow; toggle EN mempertahankan 6 kartu layanan dan 3 kartu demo.
- (16/08/2026) T30: user meminta hero actual mengikuti Minimalist Hero, bukan sekadar inspirasi. Live iframe reference dan screenshot user dibandingkan via Playwright. Hero dirombak faithful: navbar dark menyatu, background navy/black, portrait masked grayscale, circle `hero-accent`, giant headline overlap, left copy links, bottom icon/location row. Workspace inset, stats, pill CTA, dan metadata berlebih dikeluarkan dari hero. Tinggi desktop terverifikasi 836px pada viewport 1440x900; mobile 375px scrollWidth tetap 375.
- (16/08/2026) T31: API key 21st.dev diperbarui user; `get_usage` kembali 2/2 dan `get_component({id:4582})` berhasil. Source Minimalist Hero dipakai sebagai acuan struktur aktual: header handled by site Navbar, grid 3 kolom (copy/image/overlay), circle responsive 300/400/500px, entrance timing bertahap, footer location/social. Hero lokal disederhanakan ke source shape: hanya subheadline + read-more link kiri, portrait center, overlay part1/part2 kanan, icon contact + lokasi bawah. Mobile stacking diperbaiki setelah Playwright capture (375px, scrollWidth 375).
- (16/08/2026) T32: brief portfolio diperdalam memakai PRD + ui-ux-pro-max (Trust & Authority + Portfolio Grid, benefit clarity, mount stagger, reduced motion). Semua layanan diseragamkan menjadi grid 3 kolom tanpa featured card; copy diarahkan ke kebutuhan nyata (website bisnis, booking, katalog, mobile MVP, custom). Portfolio cards menampilkan service type `Landing page` + niche. `FadeIn`/`StaggerGroup` memakai render animation saat mount; interaction hanya dipertahankan pada CTA, menu, carousel, service cards/navigation, sementara informasi pasif tidak diberi hover interaction.
- (16/08/2026) T33: user mengoreksi bahwa render animation harus trigger saat elemen visible, bukan saat mount. `FadeIn`, `StaggerGroup`, dan Footer dikembalikan ke `whileInView` + `viewport={{ once: true, margin: "-80px" }}` (`viewportOnce` dari transitions.ts). Aman dari bug stuck-hidden karena item dictionary sudah punya key stabil. Terverifikasi via Playwright: konten di bawah fold mulai opacity 0 dan berubah 1 setelah scroll; Hero/Navbar tetap mount animation (above the fold, setara visible). Lint + build lolos.
- (17/08/2026) T34: user memberi foto asli `tondiki-profile-transparent.png` (1254x1254 RGBA) dan meminta dipakai di hero. `HERO_PHOTO_SRC` ditambahkan di `site.ts`; hero memakai foto transparan (bukan `tondiki-profile.png` yang tetap dipakai About). Efek hitam putih & vignette dihapus: `grayscale brightness contrast mix-blend-screen` dan mask `.hero-portrait` (CSS utility dihapus dari globals.css). Sizing diperbesar: mobile `min(76vw,22rem)` (285px), md 26rem (405px), lg 30rem; tanpa `scale-150`. Catatan: model tidak bisa membaca isi gambar.
- (17/08/2026) T35: user melaporkan foto tidak muncul di hero. Root cause dua lapis: (1) `preload` Next 16 di dev menghasilkan URL optimizer `&q=` kosong → HTTP 400 → image gagal load (diganti `loading="eager"` + `fetchPriority="high"` sesuai docs); (2) optimizer Next (sharp) mengubah PNG RGBA → palette indexed + tRNS 1-bit yang merusak alpha gradient/subjek (34% solid → ~2%). Solusi: `unoptimized` pada Image hero + file di-resize `sips -Z 700` (809KB → 303KB, alpha utuh 33.6%). Terverifikasi via canvas analysis Playwright: complete:true, solidPct 34, mobile 375px tidak overflow; screenshot `/tmp/hero-fix.png`. Restart dev server diperlukan (state Fast Refresh lama macet).
- (17/08/2026) T36: user meminta hero capture & analisa karena foto "kurang besar" dan bawahnya kasar (susah dijelaskan). Playwright screenshot dianalisa piksel: subjek foto transparan terpotong datar di tepi bawah canvas (baris terakhir y699 masih berisi subjek, berakhir garis lurus) dan warna ujung sangat gelap (RGB ~[9-14]) sehingga menyatu tapi terpotong patah. Fix: (1) akar masalah sizing = wrapper foto sebagai flex item di-shrink `flex-shrink:1` ke lebar kolom grid (405px) walau class width sudah benar — ditambah `shrink-0`; (2) diperbesar mobile `min(82vw,24rem)`=308px, md `32rem`=512px, lg `36rem`=576px (lebih besar dari circle 500px, extends 38px di bawah circle); (3) `hero-portrait-fade` mask `linear-gradient(black 72%, transparent 100%)` di globals.css — hanya ujung bawah yang larut halus ke background (bukan vignette penuh). Terverifikasi piksel per baris: transisi subjek → navy mulus 10px tanpa seam; 1440x900 hero 836px satu viewport; 375/768/1280 tanpa overflow X; lint + build lolos. Screenshot `/tmp/hero-desktop-final.png` & `/tmp/hero-mobile-final.png`.
- (17/08/2026) T37: copywriting hero diperbaiki karena audiens = calon client freelance, bukan rekruter teknis. Riset PRD (headline manfaat, hindari jargon di copy utama, persona UMKM) + ui-ux-pro-max (landing "hero conversion trust" → Trust & Authority, CTA jelas; "headline clarity" ux). "Fullstack Developer" (jargon) diganti frasa awam yang menyebut profesi: overlay headline hero → "Halo, saya Tondiki, / developer website & mobile." (ID) & "Hi, I'm Tondiki, / a website & mobile developer." (EN) — sapaan + nama + profesi dalam bahasa calon klien. Subheadline/CTA tetap manfaat + growth narrative (landing siap pakai terjangkau → sistem lebih besar). Ukuran wrap diverifikasi per kandidat langsung di h1 asli (frasa nama+sapaan+profesi render 3 baris, muat tanpa overflow X); 375/768/1024/1440 tanpa horizontal scroll; EN toggle terverifikasi; lint + build lolos.
- (17/08/2026) T38: feedback user (screenshot): tulisan headline kepotong, font kegedean, tanda baca terasa penuh. Fix: (1) font h1 dikecilkan `clamp(4rem,8vw,9rem)` → `clamp(1.75rem,2.75vw,3rem)` (115px → ±39px di 1440), leading 0.76→1.05, tracking -0.1em→-0.04em; (2) grid 3 kolom dipindah `md:grid-cols-3` → `lg:grid-cols-3` (+ order/h-full diikuti) karena di 768-1023 kolom cuma 235px → headline wrap 5 baris; sekarang tablet memakai single column 704px lapang (2 baris); (3) tanda baca dirapikan: titik di akhir baris dihapus → "Halo, saya Tondiki / developer website & mobile"; subheadline buang em-dash & koma berlebih. Hasil ukur: desktop 1440 = 3 baris (405px kolom), 1280 = 3 baris + hero satu viewport, 1024 = 3 baris, 768 = 2 baris, 375 = 3 baris; semua tanpa clipping & tanpa overflow X; EN toggle OK; lint + build lolos. Screenshot `/tmp/hero-v4-desktop.png` & `/tmp/hero-v4-mobile.png`.
- (17/08/2026) T39: feedback user "kurang gede font size overlay heronya". Font h1 diperbesar: `clamp(1.75rem,2.75vw,3rem)` → `clamp(2.25rem,3.25vw,4rem)` (46.8px desktop, 36px mobile). Terverifikasi: 1440 & 1024 & 375 = 3 baris tanpa clipping/overflow X, 768 = 2 baris (single column), hero tetap satu viewport di 1024/1440; lint + build lolos. Screenshot `/tmp/hero-v5-desktop.png` & `/tmp/hero-v5-mobile.png`.
- (17/08/2026) T40: section Layanan dipoles berdasarkan PRD §4.4 + riset ui-ux-pro-max (`Trust & Authority + Conversion`, `Pricing-Focused Landing`) + MCP inspiration pricing cards. Landing Page sekarang featured starter full-width (span penuh mulai sm, internal two-column mulai md) dengan badge amber dari token `hero-accent`, harga lama/baru, penghematan, 3 benefit PRD (responsive, WhatsApp, domain/deploy), CTA WhatsApp utama dan link ke demo. Lima layanan lain tetap konsisten dalam kartu token-based, tetapi CTA ganda redundant diganti satu tombol full-width. Header promo jadi callout card. Pesan WhatsApp sekarang kontekstual menyebut nama paket. Tidak menambah palette baru: navy authority + trust blue CTA + amber hanya sebagai penanda promo. Responsive verified 375/768/1024/1440 tanpa overflow X; EN toggle; featured shadow computed dari `--shadow-feature`; lint + build lolos. Screenshot `/tmp/services-polished-desktop.png` & `/tmp/services-polished-mobile.png`.
- (17/08/2026) T41: feedback user: tidak ingin pembedaan service card, ingin UI clean/simetris, Launch Promo sampai 31 Agustus + countdown, render/interaction/idle animation. Featured card dihapus: keenam service card sekarang identik (class dan dimensi setara, grid 3 kolom desktop/2 tablet/1 mobile). Promo dipindah menjadi banner mandiri dengan deadline `2026-08-31T23:59:59+07:00`, `<time>` semantik, countdown real-time hari/jam/menit/detik, CTA WhatsApp. Render: `FadeIn` + `StaggerGroup`; interaction: pointer tilt, hover lift, tap scale, icon hover; idle: icon float halus + promo glow/dot pulse, semuanya disabled via `useReducedMotion`. Copy promo dan label countdown bilingual dipusatkan di dictionary. Riset ui-ux-pro-max (`waitlist/coming-soon`, `countdown accessibility`) + MCP inspiration countdown card dipakai sebagai referensi; palette tetap navy/blue dengan amber highlight promo. Verified 375/768/1024/1440, card widths equal, countdown berjalan, no overflow X, EN toggle, lint + build lolos. Screenshot `/tmp/services-launch-desktop.png`, `/tmp/services-launch-tablet.png`, `/tmp/services-launch-mobile.png`.
- (17/08/2026) T42: feedback user meminta Launch Promo lebih compact dan UI card lebih memukau. Promo di-merge ke header section (bukan banner besar). Semua card tetap identik; visual diperkaya dengan shared price panel, promo label, pointer spotlight `useMotionTemplate`, idle sheen, icon float, tilt/lift/tap, dan CTA spring. Render tetap `FadeIn` + stagger; transform/opacity motion berhenti via `useReducedMotion`. Verified 375/768/1440 tanpa overflow X, enam card class identik, idle transforms aktif, lint + build lolos. Screenshot `/tmp/services-compact-motion-desktop.png` & `/tmp/services-compact-motion-mobile.png`.
- (17/08/2026) T43: feedback user: tidak ingin sheen mengkilat atau color spotlight di service card; cukup interaction pop dan idle card bergerak. Sheen/pointer spotlight dihapus, hover border/background color dihapus, tilt dihapus. Card kini idle floating seluruh elemen (`y [0,-3,0]` stagger delay per index), interaction hanya lift + scale + tap scale, CTA juga pop scale tanpa perubahan warna. Verified transform card aktif (matrix y berubah), tidak ada radial-gradient overlay, 6 card width equal, 375/768/1440 tanpa overflow X, lint + build lolos. Screenshot `/tmp/services-card-idle-final.png`.
- (17/08/2026) T44: feedback user: idle animation card dihapus total. `animate={{ y: [0,-3,0] }}` dan delay stagger per index dihapus dari `ServiceCard` (prop `index` ikut dihapus). Card sekarang statis (`transform: none` terverifikasi di browser), interaction tetap pop transform saja (hover lift + scale, tap scale). 6 card width equal, tidak ada overflow X, lint + build lolos.
- (17/08/2026) T45: user minta deep research copywriting Layanan & Portfolio + daftar layanan berbasis demand pasar. Riset: ui-ux-pro-max (`feature-rich-showcase` satu pesan kunci per card, `pricing-page-cta`/`event-conference-landing` expose deadline transparan) + web (WebUMKM/Resolusiweb/Atharyx/WebBisnismu/Fastwork/Upwork/Fiverr/Toptal): demand tertinggi jasa web/mobile = landing page, toko online/e-commerce, company profile, booking/reservasi, aplikasi mobile, custom. **"Katalog & Pesanan" diganti "Toko Online"** (harga provisional 7,5jt → promo 6,25jt, perlu konfirmasi user); custom tetap card terakhir. Copy services dirombak: title/subtitle terhubung ("Mulai dari yang dibutuhkan sekarang", konsultasi gratis), promo menegaskan "sebelum kembali normal" + deadline, deskripsi card satu pesan kunci + benefit. Portfolio: hardcode "Tiga contoh landing page untuk klinik, spa, dan wedding organizer." dihapus → subtitle generik pluggable ("Setiap contoh di bawah adalah website asli yang berjalan…"), ctaBand lebih human ("Suka salah satunya?"). Ikon `catalog`→`ecommerce` (ShoppingCart). Verified: 6 card baru di ID & EN, subtitle generik, widths equal, 375/1440 tanpa overflow X, lint + build lolos.
- (17/08/2026) T46: user minta copy lebih efektif (buang bertele-tele) + harga semurah mungkin tapi tetap relevan di bawah pasar, tidak murahan. Riset harga ID 2026 (Diginstra, Velnafa, Lazwan, Kodeweb, digitalsitepro): landing 500rb–3jt ekonomi / 5jt+ agensi; company 1,5–5jt; toko online 4–10jt; sistem 5jt+; custom 30jt+. Posisi: di atas lantai murahan (500rb–1jt), di bawah tarif agensi. **Harga promo baru (perlu konfirmasi user):** Landing 2,5→1,5jt; Company 3,5→2,5jt; Booking 6→4,5jt; Toko 7,5→5,5jt; Mobile 12→9jt; Custom mulai 8→6,5jt (diskon 19–40%). Copy dipangkas: services subtitle & promo tanpa duplikasi "kembali normal"; deskripsi card ≤~110 char satu pesan kunci; portfolio subtitle ("Setiap contoh adalah website asli yang berjalan…") & CTA band ringkas. Verified: harga baru ID/EN tampil, desc 90–114 char, 375/1440 tanpa overflow X, lint + build lolos.
- (17/08/2026) T47: persiapan deploy Cloudflare Pages (preset Next.js Static HTML Export). `next.config.ts` → `output: "export"` + `images.unoptimized: true` (next/image dipakai hero/about/gallery; optimizer gambar tak berjalan di export). Bug pra-eksis: footer `siteConfig.existingPortfolio` tak ada di site.ts → user memilih **hapus link Portfolio** dari footer. `robots.ts` & `sitemap.ts` diberi `export const dynamic = "force-static"` (wajib di Next 16 + output export). Build sukses: 4 route statis (/, /_not-found, /robots.txt, /sitemap.xml) di `out/`; lint bersih. **TODO(user):** ganti `siteConfig.siteUrl` (`src/content/site.ts:5`, masih `https://tondikiandika.vercel.app`) ke domain Cloudflare Pages/custom final — sitemap, robots, dan metadataBase ikut. Build command `npm run build`, output dir `out`.
- (17/08/2026) T48: (a) Icon WhatsApp diganti dari `MessageCircle` (chat bubble) jadi glyph resmi WhatsApp — `src/components/whatsapp-icon.tsx` (path simple-icons, `fill="currentColor"` mengikuti warna teks/CTA). Diganti di hero CTA, contact (info row + CTA), services (2 CTA card/promo), demo-showcase (askLabel + ctaBand). `MessageCircle` dipertahankan hanya untuk step "Konsultasi" di process.tsx (konsep chat, bukan link WA). (b) Email dibuat fungsional: tombol mailto diganti **form kontak** (nama, email, pesan) di `contact.tsx` yang menyusun `mailto:` terisi (subject+body) lalu membuka klien email pengunjung — berfungsi tanpa backend/secret (static export). Label form bilingual di dictionary (`contact.form`). Catatan: kirim langsung via server butuh email provider (Resend/Web3Forms/Formspree) + API key — belum dilakukan. Verified: glyph WA tampil, form 3 field ada, no overflow X; lint + build lolos.
